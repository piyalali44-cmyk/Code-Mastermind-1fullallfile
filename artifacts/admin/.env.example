# ─── StayGuided Me — Admin Panel ──────────────────────────────────────────────
# Copy this file to .env.local and fill in your values before running.
# Never commit .env.local to version control.

# Supabase (required)
VITE_SUPABASE_URL=https://tkruzfskhtcazjxdracm.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# API server URL (optional — defaults to relative /api if admin and API run behind same proxy)
# Set this if admin and API server are on different domains
# VITE_API_BASE_URL=https://your-api-server.com/api
